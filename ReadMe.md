HTML (Tags -> element : properties/attributes)
CSS (Rules to design a Html page, properties)
selector{
    property:value;
    property:value;
    property:value;
}
color, background, font, text, gradient(lienar,radial,conic)